# Copia a .env y completa (no subir .env al repositorio)
# Valores por defecto en código apuntan a clinicos-aed47
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=

# IA — proxy Vercel protegido (api/chat.js + DEEPSEEK_API_KEY en Vercel, no en el repo)
# Requiere sesión Firebase (médico o clínica). Sin login no funciona = nadie externo puede abusar.
VITE_AI_PROXY_URL=https://clinicos-doc.vercel.app/api/chat
VITE_AI_PROVIDER=deepseek
